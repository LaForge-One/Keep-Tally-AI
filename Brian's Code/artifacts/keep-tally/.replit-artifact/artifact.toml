kind = "web"
previewPath = "/"
title = "KeepTally"
version = "1.0.0"
id = "artifacts/keep-tally"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25869

[services.development]
run = "pnpm --filter @workspace/keep-tally run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/keep-tally", "run", "build" ]
publicDir = "artifacts/keep-tally/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25869"
BASE_PATH = "/"
